import React, { useState } from 'react';

const Dropdown = ({ items }) => {
  const [activeIndex, setSelectedItem] = useState(null);

  return <div className="ui"></div>;
};

export default Dropdown;
